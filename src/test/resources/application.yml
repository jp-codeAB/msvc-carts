spring:
  datasource:
    url: jdbc:h2:mem:cartdbtest
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: create-drop
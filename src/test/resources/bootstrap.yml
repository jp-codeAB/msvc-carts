spring:
  application:
    name: msvc-carts
  cloud:
    config:
      enabled: false
      import-check.enabled: false
      discovery.enabled: false
  main:
    allow-bean-definition-overriding: true

eureka:
  client:
    enabled: false
